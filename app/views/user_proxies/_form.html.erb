<%= form_for(@user_proxy) do |f| %>
  <% if @user_proxy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_proxy.errors.count, "error") %> prohibited this user_proxy from being saved:</h2>

      <ul>
      <% @user_proxy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
